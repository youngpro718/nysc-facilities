# Production Environment Configuration
# NYSC Facilities Management System
# Copy this to .env.production and fill in your production values

# IMPORTANT: Never commit .env.production to version control
# These are example values only - replace with your actual production credentials

# Log Level - Set to 'error' to hide console.logs in production
VITE_LOG_LEVEL=error

# Supabase Configuration
# Get these values from your Supabase project dashboard
# Project Settings → API → Project URL and anon/public key
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_production_anon_key_here

# SECURITY NOTES:
# 1. Use different Supabase projects for development and production
# 2. Rotate keys regularly (every 90 days recommended)
# 3. Never expose service_role key in frontend code
# 4. Set environment variables in your deployment platform (Netlify/Vercel)
# 5. Enable Row Level Security (RLS) on all tables
